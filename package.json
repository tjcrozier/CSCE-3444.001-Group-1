{
  "name": "echo-code",
  "displayName": "Echo Code",
  "description": "Reads Python errors aloud using Pylint for visually impaired students.",
  "version": "1.0.0",
  "publisher": "Slitchie",
  "engines": {
    "vscode": "^1.97.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:python",
    "onSave"
  ],
  "main": "./extension.js",
  "extensionDependencies": [
    "ms-python.python"
  ],
  "contributes": {
    "commands": [
      {
        "command": "echocode.readErrors",
        "title": "Read Python Errors Aloud"
      }
    ],
    "configuration": {
      "properties": {
        "docstringGenerator.huggingFaceApiKey": {
          "type": "string",
          "default": "",
          "description": "Add Hugging Face API key to your settings.json file."
        }
      }
    },
    "keybindings": [
      {
        "command": "echocode.generateDocstring",
        "key": "ctrl+alt+space",
        "mac": "cmd+alt+space",
        "when": "editorTextFocus && editorLangId == 'python'"
      }
    ]
  },
  "scripts": {
    "lint-python": "pylint **/*.py"
  },
  "devDependencies": {
    "@types/vscode": "^1.97.0"
  },
  "dependencies": {
    "axios": "^1.8.3",
    "dotenv": "^16.4.7",
    "say": "^0.16.0"
  }
}
